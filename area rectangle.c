//To determine the area of ​​rectangle
#include<stdio.h>
int main()
{
    float length,width,area;
    printf("enter length =");
    scanf("%f",&length);

    printf("enter width =");
    scanf("%f",&width);

    area=length*width;
    printf(" rectangle of area= %.2f",area);

    return 0;
}

