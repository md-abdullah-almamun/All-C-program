//conversion between hexa-decimal to decimal

#include<stdio.h>
int main()
{
    int number;
    printf("hex-decimal number=");
    scanf("%x",&number);

    printf("decimal number = %d",number );
    return 0;

}
